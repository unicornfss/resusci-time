# Do not set VITE_TRUST or VITE_BUILD_CHANNEL here — Vite loads this file
# after .env.[mode] in dev and would override wmas-preview / standard-preview.
# Trust and channel come from the --mode flag (see vite.config.ts).
